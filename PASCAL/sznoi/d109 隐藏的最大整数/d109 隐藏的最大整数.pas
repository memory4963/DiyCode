var
a,b:string;
n,m,i:integer;
temp:char;
t:array[1..1000]of string;
begin
repeat
  read(temp);
  if (ord(temp)<=ord('9')) and (ord(temp)>=ord('0')) then
   while (ord(temp)<=ord('9')) and (ord(temp)>=ord('0')) do
   begin
   i:=i+1;
      read(b[i])
      end;
 if



