#include<stdio.h>
#include<string.h>
int NUM(char str[], int t);
int Answer(int n, int num1[n], int num2[n / 2 + 1], int m)
int Printf(char str[])
int mmain()
{
	int i, a = 1, m = 1;
	long long int n, sum = 1, answer;
	char a = 'a', b = 'b';
	char str[];
	while (scanf("%s", str[]) != EOF)
	{
		Printf(str[]);
	}
	return 0
}
int Printf(char str[])
{
	n = strlen(str[]);
	int number1[n], number2[n / 2 + 1];
	NUM(str[], n);
	answer = Answer(n, num1[n], num2[n / 2 + 1], m);
	printf("%lld\n", answer);
}
int NUM(cahr str0[], int t)
{
	int j;
	for (j = 0; j <= n = 1; j++)
	{
		num1[j] = (int)(str0[j] - 'a')
	}
	return 0;
}
int Answer(int n, int num1[n], int num2[n / 2 + 1], int m)
{
	int k = 0;
	if (num1[] == {0, 1})
		return m;
	else if (num1[] == {1, 0})
	{
		return sum + m;
	}
	sum = sum * 2;
	if (num1[0] == num1[1])
	{
		num2[0] = 1 - num1[0];
		k = 1;
		m == m + 1;
	}
	for (i = k; 2i - k <= n; i++)
	{
		while (2i <= n)
		{
			if (num1[2i - k] == num1[2i - k + 1])
			{
				return -1;
			}
		}
		num2[i] = num1[2 * i - k];
	}

	Answer(int n / 2 + 1, int num2[n / 2 + 1], int num3[(n / 2 + 1) / 2 + 1], int m)

		return sum + m;
}